@model  MyWoodenHouse.Client.Web.Areas.Administration.ViewModels.Products.ProductCompleteViewModel

<div class="modal-dialog">
    <div class="modal-content">

        <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
            <h3>
                Are you sure you want to delete
                @Html.DisplayNameFor(model => model.ModelName)?
            </h3>
        </div>
        <div class="modal-body">
            @Html.LabelFor(c => c.Id) : @Html.DisplayFor(c => c.Id)
            @Html.LabelFor(c => c.Name) : @Html.DisplayFor(c => c.Name)
            @Html.LabelFor(c => c.Description) : @Html.DisplayFor(c => c.Description)
        </div>

        @using (Html.BeginForm("Delete", "Products", FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            @Html.HiddenFor(x => x.Id)
            <div class="modal-footer">
                <button type="reset" class="btn btn-success" data-dismiss="modal">
                    <span class="glyphicon glyphicon-remove"></span>
                    Cancel
                </button>
                <button type="submit" id="approve-btn" class="btn btn-danger">
                    <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
                    Delete
                </button>
            </div>
        }
    </div>
</div>

@section scripts{
    <script type="text/javascript">
        $(function () {
            $('#approve-btn').click(function () {
                $('#modal-container').modal('hide');
            });
        });
    </script>
}